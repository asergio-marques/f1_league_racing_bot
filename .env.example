BOT_TOKEN=
DB_PATH=bot.db
# The scheduler's job store, kept in its own file so its writes cannot contend with the
# league database. Leave it unset to place scheduler.db beside DB_PATH.
SCHEDULER_DB_PATH=
